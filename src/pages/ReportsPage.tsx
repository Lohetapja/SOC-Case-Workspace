import { SectionPlaceholder } from '../components/SectionPlaceholder'

export function ReportsPage() {
  return (
    <SectionPlaceholder
      title="Reports"
      subtitle="Generate a clean Markdown report of the full investigation."
      milestone="Milestone 8 — Markdown report export"
    />
  )
}
