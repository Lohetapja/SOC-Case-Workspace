import { SectionPlaceholder } from '../components/SectionPlaceholder'

export function TimelinePage() {
  return (
    <SectionPlaceholder
      title="Timeline"
      subtitle="Reconstruct the sequence of events into a coherent narrative."
      milestone="Milestone 4 — Timeline builder"
    />
  )
}
